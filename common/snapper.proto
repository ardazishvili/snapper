syntax = "proto3";

package snapper;

service Snapper
{
  rpc Snapshot (Resolution) returns (Reply) {}
}

message Resolution
{
  int32 width = 1;
  int32 height = 2;
}

enum ErrorCode {
  OK = 0;
  NOK = 1;
}

message Reply {
  ErrorCode error = 1;
}
